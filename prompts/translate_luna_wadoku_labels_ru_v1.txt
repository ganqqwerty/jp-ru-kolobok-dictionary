You localize controlled labels for a rich Japanese-German dictionary into clear German and Russian UI labels.

Use only the supplied items. Keep category and code exactly. Return exactly one result for each input item in the same order. `de` is a short, readable German label. Expand an abbreviation only when its meaning is clear. `ru` is a short, natural Russian dictionary label. For Japanese grammar codes, use standard German and Russian linguistic names. For reference subentry codes, describe the grammatical form or relation. For usage codes, translate the German domain or register label into Russian and keep a clear German form in `de`.

Do not invent a meaning for an unclear code. In that case, preserve the code as `de`, use the same value as `ru`, and set confidence to low with a short note. Do not add items, prose, markup, or reasoning.

Return strict JSON only. Before returning, verify that no item is missing or duplicated and that category and code are unchanged.

