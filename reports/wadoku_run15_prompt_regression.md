# WR15 — Source-unit binding regression

WR16-28 — New rich commands now use config.wadoku.rich.luna.toml (v3), not the historical v2 profile. Prompt paths come from its rich.prompts table: translation v8, review v9, classification v7, transcription v1. Resuming translation resolves the original file by the run's frozen hash; an explicit mismatched prompt fails. Run 16 prepare-only successfully reused frozen v7 with zero new batches or Luna calls, and status still reports 193 articles / 940 accepted units. The old v2 config is unchanged. Added WADOKU_RICH_RUNBOOK.md and profile regression tests; full tests pass.

WR16-26 — Primary review completed for the full pilot. Review v9 then rechecked six run-local articles (3,79,93,113,124,199), 64 units, plus the remaining uncertainty queue. It corrected a German reflexive participant reversal and a meaning copied from a neighboring sense, and resolved valency-only warnings. All 940 units now have accepted latest translations. Acceptance of the unqualified negative meaning of 素晴らしい remains a linguistic caveat, not external confirmation. Grouping/example reconciliation and user-side Yomitan verification are still open; no release approval is claimed.

WR16-27 — Added bounded article-subset recheck and --resume for existing requests. Subset identity and directory prevent dispatching neighboring queues; out-of-run IDs and implicit subset review are rejected. PostgreSQL tests cover scope and idempotence. Fixed a real contract mismatch: the rich review output schema allowed null reason while ingestion rejected it. It now requires a nonempty string without changing Jitendex. Runtime prompt and schema files/hashes are saved per attempt. The resumed final request passed the corrected schema. Full test suite and isolated PostgreSQL tests passed.

WR16-23 — Built dist/wadoku-rich-pilot193-diagnostic.zip through the shared PostgreSQL export path: all 193 source entries, 190 lexical owners, 443 lookup rows, 390 pitch rows, three schema-validated banks. SHA-256 f1127e27f958cceabe249af42913bf693ad2ca17d23acfcb6427cbbdf39204a0. Explicit diagnostic mode relaxes translation acceptance only; it still rejects missing coverage and uses the same structural assembly. Its title says НЕ ПРОВЕРЕНО, release_approved=false, and the companion JSON records 81 unaccepted units at export time. A repeatable-read snapshot keeps targets and status consistent during concurrent review. Ordinary export remains acceptance-gated. This archive is not completed-pilot evidence.

WR16-24 — Browser policy denied access to the Yomitan extension settings page. No alternative route was used to bypass that denial. Requested user-side import/search/link verification of the diagnostic ZIP; popup validation remains unverified. This does not block continuing script and language checks.

WR16-25 — Review v8 tests explicit Japanese-source priority for directly stated participants, quantities, temporal relations and technical referents. It resolved the temporal, medical and baseball disagreements while preserving their German conflict in review reasons. Two valency-only warnings still persisted. The reviewer also accepted an already questionable reflexive German translation; acceptance counts alone remain insufficient. Default review prompt was not promoted and known quality issues are not declared solved.

WR16-21 — Full run 16 translation coverage reached 193 source articles and 940/940 units through twenty saved windows. All windows have source-bound analysis. This is translation-stage completion, not dictionary approval: contextual review remains incomplete, as do semantic ownership/example reconciliation and final full-pilot Yomitan checks. No manual target edits were used to close coverage.

WR16-22 — Translation and review retries now include bounded feedback from the latest rejected attempt of that batch. The original manifest and base prompt stay unchanged. Each new attempt saves its runtime prompt and both hashes; the full prompt enters the context reservation. Review rejection records now retain detailed validator issues instead of only a generic replacement error. Tests cover batch isolation, latest-error selection, feedback size, saved hashes, bounded retry and PostgreSQL ingestion. Existing retry limits remain. Live review continues; this change does not claim that every semantic exception is resolved.

WR16-17 — Fixed a validator defect, not dictionary text: source-marked scientific terms in main senses now receive the same treatment as examples. The check requires every selected XML path to exist and have a scientific ancestor; a neighboring annotation is not enough. Added revalidate-rejected: it validates an unchanged saved response, claims a new attempt, preserves rejected history and records zero new model tokens. Run 16 recovered three missing units this way. Unit tests and isolated PostgreSQL recovery tests pass.

WR16-18 — Review prompt v7 removes the rule that required a separate Japanese example to resolve every warning. A 12-unit unresolved cohort produced eight accept/replace decisions and four remaining doubts. Both concrete source conflicts (technical referent and temporal relation) remained unresolved. Two valency warnings still repeated despite the new rule. One additional newly queued unit was accepted. Some replacements still expand or simplify synonyms too aggressively: fewer warnings do not prove better wording. No manual target edits and no default-prompt promotion.

WR16-19 — New rich review requests now sort units by the frozen source projection, even for legacy translation runs. One saved request showed the old 0,1,11,2 order. A PostgreSQL regression deliberately reverses the input envelope and checks source-ordered review output and stable repeat preparation. Existing request files remain unchanged. Window analysis now records errors plus a bounded successful sample instead of rewriting individual articles.

WR16-20 — Rich response validation now pairs results by unique unit ID when coverage is exact; the original response file stays unchanged. Reordering an otherwise complete array no longer costs a new Luna call. Missing or duplicate IDs and wrong source hashes still fail. PostgreSQL tests exercise reordered ingestion and all three invalid cases. Window 19 recovered its actual failures through the existing bounded retry path; it has all 135 units. Its quality sample still exposes copied domain labels and explanations duplicated in definitions, so a valid batch is not a release-quality claim.

WR16-14 — Windows 11–15 completed and have main-thread source/target analyses in PostgreSQL. Window 16 runs in session 13434; normal review session 34777 was last confirmed live. The new unresolved-only review mode selects latest unaccepted candidates with prior needs_adjudication, includes those reasons, and retains review history. Review v5 repeated all processed doubts; v6 resolved some normal-polysemy cases but left genuine or still-unclear conflicts. Do not equate a reviewer acceptance with final linguistic verification.

WR16-15 — Added a bounded transcription-resolution script using shared Luna CLI, PostgreSQL batch/attempt records, exact XML paths and source hashes. Luna resolved the remaining wo/o, wa and nani occurrences; re-running sent no new requests. Source-paired romanizations resolve without an LLM when XML provides an unambiguous Japanese counterpart. No target text or source XML was edited manually. The parser uses stored path-bound decisions; tests reject wrong identity, coverage and non-Japanese targets.

WR16-16 — The assembly-preflight command now exercises the same reference/ownership path as export, using source text and explicitly disclaiming translation QA. The full 193-entry run passes with zero structural errors, 190 owners and 443 term rows. Report: work/wadoku-xml/pilot-v6/assembly-preflight-run16.json. Source-only pronunciation limitations remain explicit. Exact single-form reading alignment resolves a previously flagged suffix variant; references to leading-slot templates use their literal fixed suffix, not an invented complete word. Final full-pilot translated archive and Yomitan/semantic checks remain unfinished.

WR17-2 — Run 17 completed all 38 translations and contextual reviews. Main-thread comparison found that v8 removed glossary German echoes, but copied Hund in one example; review v5 corrected it and an ungrammatical Russian example. The medical Japanese/German disagreement remains documented despite review acceptance. This proves a bounded translate/review loop, not full dictionary quality. Built dist/wadoku-rich-run17-diagnostic.zip through PostgreSQL export-candidate: eight source entries, eighteen term rows, thirteen pitch rows, schema validation passed; SHA-256 f575f6f7664df91ed4260b96984c3b5dda30de76b9a1e6c31199267e66af5e6a. No manual target edits.

WR16-13 — Windows 9 and 10 have saved source-bound analysis; window 11 runs in session 7314 and review of window 10 runs in session 68875. The full 193-entry run remains on frozen translation v7 with review v5; v8 is a separate diagnostic, not silently substituted. Export now loads exact latest accepted coverage, applies lexical ownership and classified lookup aliases, and writes a schema-checked candidate without overwriting old artifacts. Open-template pitch remains source-only because it cannot safely transfer to a suffix or expanded word. Cross-entry multi-pitch sense alignment still requires review. Full pytest passed with three skips after these changes; isolated PostgreSQL rollback/assembly-loading tests passed separately earlier. Full-pilot archive, unresolved semantic cases and real Yomitan validation remain unfinished.

WR17-1 — Run 17 tests translation prompt v8 on eight source entries from the same 193-entry pilot: 64967, 180002, 2069333, 3512808, 5012593, 6051433, 6154617, 6910628. The new candidate-subset option preserves candidate example context without claiming example approval. All 38 source units match run 16 by article, source path, role and source hash. This diagnostic run does not replace the full pilot. Prompt v8 adds a general ban on copying ordinary German words into Russian targets, while retaining protected material and genuine scientific names or etymons. Run 17 also uses the already implemented source-order fix; a comparison cannot attribute every difference to the prompt alone. Session 36573 is running; no full quality result yet.

WR16-12 — Translation window 9 finished and remains awaiting analysis; run 16 has 90 translated source articles. Review session 37891 is processing its existing backlog with review v5. The review driver now rolls back partial writes on invalid responses, records rejection and retries at most three attempts without generic translation-manifest splitting. Stale leases cannot be requeued by this path. SQLite tests and a real isolated PostgreSQL test verify rollback, persisted rejection and successful re-claim. No manual translation edits were made.

WR16-11 — Seventh window completed: seventy source articles/333 translated units total. Read all 38 window pairs and saved its analysis. Added bounded parallel translation within the existing window: default three workers, maximum five, each with its own database connection and persisted result. Tests prove the concurrency cap, no duplicate dispatch, and completion of sibling workers when one fails. Window 8 is the first real exercise, session 54446; inspect before claiming it completed. Existing per-request context checks, frozen membership and retry bounds remain. Review remains a separate bounded process.

WR16-10 — Traced the Latin rejection to an explicit source annotation: the selected child trans has langdesc=scientific. Added a source-path-bound check to translation and replacement validation. It permits source Latin tokens only when that selected XML fragment has the annotation; ordinary German words do not gain permission. Tests cover the marked path, an unmarked sibling, and a generic Jitendex context; 21 focused/shared tests pass. The retried translation was accepted before this validator change loaded in its running process, so do not attribute that success to the new rule. The original response remains in history. Session 8586 continues the fifth window with persisted rejection recovery.

WR16-9 — Fifth window stopped on ValidationFailure. The driver had rolled back ingestion's rejection records, leaving a terminal worker's attempt claimed and batch leased. Replayed the unchanged saved response, committed the rejection, and used shared retry_or_split to requeue only that batch. The actual issue is too_much_english on a Latin legal phrase with a Russian explanation; no validator bypass or manual target correction was applied. The driver now commits deterministic rejection records and uses bounded shared retry/splitting before continuing frozen window membership. Resumed the same window; prior accepted batches are not resent. Source-echo diagnostics now also catch unparenthesized copied words such as Heim inside Russian text; tests cover pure Latin scientific names and protected tokens remaining unflagged.

WR16-7 — Added reorder-pending to apply source ordering without discarding run 16. It locks the run and ready batches, skips every assigned or attempted batch, validates the original manifest hash, writes a new deterministic manifest and records its parent in audit. Original files remain; original batch rows become blocked/superseded. Applied eleven replacements. A second invocation changed zero batches. Independently compared all eleven original/replacement pairs: unit content and article context are identical; only unit order and manifest identity change. No accepted translations were retransmitted. These replacement requests still need real Luna execution.

WR16-8 — Third translation window completed with ten articles/78 units, bringing run 16 to thirty articles/175 units. Session 33647 is terminal. Session 14062 now reviews the eight remaining articles/57 units from that window. Its four earlier unresolved review cases remain in history; next steps are window analysis, continuation and explicit exception handling, not manual target edits.

WR16-5 — Run 16 third-window 掛ける repeated the shifted-sense failure despite translation v7. Inspecting its frozen manifest exposed unit order 0,1,10,11,…,2,… from lexicographic database pointers, while source-tree senses remain numeric. The two wrong meanings match earlier numeric senses. This is evidence for a contributing input-order defect, not proof that order explains every model error.

WR16-6 — Preparation v4/source-order envelopes now sort requested units by their frozen projection order. A 12-sense regression verifies numeric order and unchanged legacy envelopes; thirteen focused tests pass. Existing run-16 manifests remain immutable and still use the legacy ordering. The fix has not yet been tested in a real Luna dispatch; an audited replacement of pending requests or a separately versioned regression is still needed. Session 33647 remains the third-window translation; session 70930 completed four review batches with four needs_adjudication cases across に and 掛ける. Do not claim those cases resolved.

WR16-4 — Added the read-only status command to wadoku_rich.py. It reports whole-run article/unit coverage, missing units, latest accepted targets and translation/review/window states without migrations or model calls. Verified it against real pilot PostgreSQL: 193 articles and 940 units, not just the latest ten-article window. A regression proves that a newer unaccepted replacement cannot be hidden behind an older accepted translation. Session 77299 remains the live v5 recheck; three batches have completed at the last poll. Do not start a duplicate recheck.

WR16-3 — Session 35695 completed the remaining 30-unit review: 29 accepted/replaced, one needs_adjudication. Review v4 accepted copied German parentheticals in 表. Added immutable review v5 with an explicit distinction between preserving meaning and copying German wording; scientific names, actual etymons and protected material remain exempt. Started a version-bound recheck of all 63 window units, without rerunning translation or editing targets. Angeber in 犬 also remains questionable after v4; do not treat accepted status as proof of semantic quality. Run 16 window 1 still awaits final analysis before further translation windows.

WR16-1 — Run 16 window 1 completed: ten articles, 63 units. Main-thread reading found that v7 removed redundant Tōfu from 壁, but 表 now copies German source words in parentheses. 犬 renders ambiguous Angeber as хвастун; this needs contextual review. Do not create another full run merely for these observations: test review v4 first.

WR16-2 — Session 84661 completed review of the first seven available articles, 33 units. A subsequent review invocation handles the remaining three. The window remains awaiting analysis until those results are inspected. No manual targets changed. source_echo_warnings now exposes copied Latin-script parentheticals in the window report even at high confidence. It is advisory because names and etymons may be legitimate. Eleven focused tests pass.

WR15-1 — Run 15 completed its first ten-article window: 50 translated units, no missing units. The main thread read all 50 source/target pairs. Technical ingestion did not detect semantic substitution in high-confidence answers.

WR15-2 — In 見る, überprüfen/nachschlagen became читать/просматривать; auf etw. achten/sich um etw. kümmern became заметить/увидеть; betreuen/sich um jmdn. kümmern became рассматривать/наблюдать. These answers fit other senses of the headword but not their own source units. 壁 also contained redundant Tōfu after тофу. These are general prompt defects, not reasons to hand-edit entries.

WR15-3 — Immutable translation v7 adds source-unit binding and a final check against the selected source array. Review v4 checks the same failure class and redundant Japanese romaji. Neither prompt contains the erroneous articles or their desired answers. Run 15 window 1 has a saved stopped analysis; do not dispatch its remaining v6 translation batches.

WR15-4 — Review v4 processed all 22 見る units. PostgreSQL shows automatic replacements for the three shifted meanings: проверять/искать в справочнике; обращать внимание/заботиться о чём-либо; заботиться о ком-либо/ухаживать. This demonstrates detection of the observed failure class. It does not prove complete quality: the awkward grammar gloss about the -て construction remained accepted.

WR15-5 — Run 16 is the full v7 successor, not a smaller pilot: 193 articles, 940 units, identity 902eef7ef13acbfbc3ab314a28b2c29a0801f6bf4c46d59cfd35d4ad719638ac. Session 68621 runs its first ten-batch window. Session 19551 reviews four run-15 batches with v4; five review batches were prepared from the then-available 38 units. Both were live at the last check. Session 35338 completed run 15 window 1. No manual translation patches were applied.
