# WRI — Rich Wadoku implementation and pilot

WRI-1 — Started on 2026-09-10. The user authorized implementation, prompt changes, Luna CLI translation and validation, and a PostgreSQL pilot of 150–200 source articles. This supersedes the earlier execution hold for this pilot only. Full-dictionary translation is not authorized by this request.

## WRI-STATE — Current evidence

WRI-STATE-1 — Created two new databases in the existing local PostgreSQL server: `wadoku_rich_pilot` and `wadoku_rich_test`. Existing translation databases were not migrated or changed. The pilot database now holds the frozen scope and initial classification attempts; it has no new Russian translation run yet.

WRI-STATE-2 — Migration 0011 adds frozen per-run projections and bounded window state. `wadoku_pipeline.py` prepares selected scopes through the new `wadoku-xml-v3` contract. Source article JSON remains unchanged. Run-scoped unit IDs retain a separate semantic identity for future exact reuse. Whole-sense glossary arrays and example units remain distinct.

WRI-STATE-3 — The shared batcher now reads frozen rich projections and supplies complete source/sense context. Initial packing and retry splitting preserve meaning packets. An indivisible oversized packet fails rather than losing context. The shared response validator supports typed Wadoku glossary arrays without using Jitendex-specific English rules.

WRI-STATE-4 — `wadoku_windows.py` stores N root batches per window, includes split descendants by unit membership, reports missing units and unknown usage, and requires an actual source-bound analysis before the next window. It does not require a user approval field. The dispatch coordinator, budget enforcement and saved-response recovery still need integration; this module alone is not a working runner.

WRI-STATE-5 — A real PostgreSQL fixture passed preparation, repeat preparation, shared batching, claimed-attempt response ingestion and Yomitan row rendering. It also tested the window boundary, stale-analysis rejection and continuation. The fixture uses a clearly labelled synthetic response; it makes no linguistic quality claim. Test: `tests/test_wadoku_pipeline_postgresql.py`, database `wadoku_rich_test`.

WRI-STATE-6 — Four focused pure tests passed for projection identity, exact target coverage, complete-context envelopes, indivisible packets and typed glossary validation. Existing Wadoku regression tests also passed. Luna classification started after those checks; results follow below.

## WRI-SCOPE — Frozen PostgreSQL pilot scope

WRI-SCOPE-1 — Scope `a159ede6979b17ddd63ab0aefcaa1ce36ef8750c66746ac6e082adba39d64ea2` contains 190 source entries: the original 150, 13 grammar/lexical additions and 27 reference dependencies. Core categories include 75 Kaishi entries and 10 names. Full-XML inventory found 1,115 example candidates for the selected parents. It took 50.78 seconds. PostgreSQL stores entries and candidates separately; `work/wadoku-xml/pilot-v6/scope.json` is a derived inspection artifact.

WRI-SCOPE-2 — Of 85 outgoing reference records, 46 target included entries and 39 deliberately remain external to the bounded pilot. None has a missing source target. This does not prove that all links work in an isolated Yomitan profile; the later lookup audit must report the external subset.

## WRI-CLASS — Initial Luna classification windows

WRI-CLASS-1 — Five actual Luna CLI calls processed three distinct focal entries. PostgreSQL records 83,976 input tokens and 4,549 output tokens, 88,525 total. Three proposals passed the structural validator; two failed and remain in history. These are classification calls, not Russian translation workers. All five calls are terminal; none remains leased.

WRI-CLASS-2 — 知らない (4923103), attempt `att-28bc6108c6774576b14e620c9d94437b`, correctly proposes inheritance from 知る (9858285) and deinflection. The main thread read the source senses and result. This does not yet prove native Yomitan deinflection or preservation of child-specific meanings after grouping.

WRI-CLASS-3 — ほだし (7271622), first attempt `att-3e58a1863c184f67b89a988de11e32f8`, correctly proposed an independent noun but incorrectly copied child examples into focal-entry lookup aliases. The validator rejected it. Prompt v3 and the per-entry schema prohibit aliases when the focal entry has no template. Retry `att-b9ad1ecb3189419faa7d40c713ae7219` passed with empty aliases and three separate example selections. The main thread read all eight candidates and decisions against source blocks. A family-burden candidate has a potentially misleading German subject direction; it was not selected and should not be promoted without review.

WRI-CLASS-4 — 知らない人 (10084606), attempt `att-df211fce5108403db6562ed74cc5938a`, proposed an independent phrase but filled parent_id with the source reference rather than leaving ownership null. The validator rejected it. Prompt v4 makes that distinction explicit. Retry `att-7fd5666dabdb4136941f899e27b3dca5` passed as independent with null parent ownership. Historical prompts v2/v3 and failed responses remain unchanged.

WRI-CLASS-5 — The shared CLI dispatcher and PostgreSQL batch/attempt journal handle these calls. The new command limits each invocation and stops on a rejected response. It stores proposals as `luna-proposal`, not human approvals. Three of 190 entries and eight of 1,115 example candidates have proposals. Remaining decisions, their adoption into the translation projection, robust classification recovery and the full translation-window driver remain unfinished.

WRI-CLASS-6 — Request sizing now reserves 32,768 tokens for CLI/runtime instructions in addition to a conservative byte bound for supplied text/schema and a separate output reserve. The first window exposed significant fixed runtime input overhead. Later calls compare actual usage with the reservation and stop on excess. This is a conservative operational estimate, not a claim of knowing every hidden runtime token in advance.

## WRI-NEXT — Remaining work toward the actual goal

WRI-NEXT-1 — Scope freezing and full reverse-example inventory are complete: see WRI-SCOPE. Keep the 190-entry scope unchanged while completing decisions, translation and review. The 39 external reference records still need explicit treatment in the final lookup audit.

WRI-NEXT-2 — Implement source-bound grouping, lookup and example decisions in PostgreSQL. Use Luna for ambiguous lexical classification, not string heuristics. Add examples only after deciding their source meaning and attachment. Use the existing three-example default with a reasoned override up to five; reconcile the contradictory older five/seven rule.

WRI-NEXT-3 — Complete the single prepare→dispatch→ingest→accept→replace→export path, including direct-command protections, exact context reuse, exact example identities in the ZIP, and the three simplified barriers. The legacy main exporter and CLI quality ledger still require integration. Do not present the new fixture as proof those parts work.

WRI-NEXT-4 — Add a versioned prompt and conservative full-request budget checks, then execute small Luna windows. Analyze errors and a source-grounded sample after each window. Review all pilot articles in the main thread, use Luna validation where useful, fix observed problems and rerun affected work. Keep actual attempts, usage and editorial provenance.

WRI-NEXT-5 — Build the test ZIP, independently check its data and example attachments, and test actual Yomitan lookup/rendering for spelling variants, inflections, phrases, links, pitch scope and examples. Report unresolved limitations honestly. Completion requires this real pilot result, not only tests or a valid schema.

## WRI-CLASS2 — Further windows and deterministic repairs

WRI-CLASS2-1 — Completed another 20 classification calls in bounded windows and then two large-entry calls. Five-way concurrency now drains every submitted response before returning, even if one response fails. At this checkpoint 23 of 190 entries have proposals and 13 of 1,115 candidate records have decisions. Both large-entry calls failed validation and are terminal; no classification process remains live.

WRI-CLASS2-2 — Three rejected responses were recovered without new Luna calls. Entry 906 added full-width spacing to kana; entry 22898 dropped the fixed particle を from its suffix; entry 34262 included a display heading with the editorial × sign. Normalization records each old and new value. The fixed suffix comes literally from the non-display XML spelling and reading, only after the model identifies the policy as suffix_only. This does not infer a lexical classification or invent a completed phrase. Historical rejected attempts stay rejected; separate revalidation audit events explain the adopted proposals.

WRI-CLASS2-3 — Classifier request v4 sends the full focal/parent XML once instead of both a verbose JSON tree and duplicate block text. Candidate meanings retain their paths and protected fragments. Tests verify preserved text, attributes and order. The response schema version and request version enter task identity. Existing frozen requests remain readable.

WRI-CLASS2-4 — The largest candidate sets are 頭 (3512808), 72, and 見る (9337937), 46. A local model-cache check returned context_window 272000 and effective_context_window_percent 95 for gpt-5.6-luna. These two requests use a 128000 budget, below half the reported effective context; the normal default remains 96000. Both include the 32768 runtime margin and an explicit output reserve. Actual usage exceeding a reservation stops adoption. A cache value is operational evidence, not a guarantee that future model settings never change.

WRI-CLASS2-5 — New tests cover lossless XML context, audited kana spacing, preservation of fixed particles, exclusion of display-only headings, and rejection of examples copied into parent aliases. Russian translation, full grouping/export integration, all-entry main-thread review and real Yomitan checks remain unfinished.

WRI-CLASS2-6 — The large calls did not exceed their token reservations. 見る used 24,487 input and 4,600 output tokens in 86.65 seconds; 頭 used 29,766 input and 5,792 output tokens in 108.65 seconds. However, the 46-item response repeated four IDs and omitted four; the 72-item response repeated 29 IDs and omitted 29. The latter also confused suitability with the three-example display limit. Both responses remain rejected. This is an output-contract/task-complexity failure, not evidence of context exhaustion.

WRI-CLASS2-7 — Next repair: make every candidate a required object property in the response schema rather than allowing repeated IDs in an array. Separate eligibility from the bounded display selection, and preserve unresolved judgments. Do not invent missing decisions or simply reorder a response with missing/duplicate IDs. Then retry only the two rejected large tasks and resume bounded windows. Their attempt IDs are `att-97b0bb93a23146adbba6f0b72213cef4` and `att-ace8f2d4d7644d8bb05f2a07a11a1496`.

WRI-CLASS2-8 — Implemented the required-property map and separate selection in prompt v5 and response schema v4. Both retries passed contract checks: 見る attempt `att-87ced1c8270d4349821d3583b693a3aa` used 25,566 input and 4,579 output tokens; 頭 attempt `att-d79b337b33824883ac873028fc850ef6` used 31,401 input and 6,029 output tokens. Each selected three examples. 見る retained four needs_review candidates with open slots. These are proposals, not proof of semantic correctness or completed example integration. The full pytest run completed successfully with three skips; skipped tests are not evidence of PostgreSQL validation.

WRI-CLASS2-9 — Continued with the next window of ten pending scope entries using the same classifier and five-way concurrency. Review its terminal results before issuing another window. The two large entries still need source-grounded review of selected examples and unresolved candidates before their proposals become export decisions.

WRI-CLASS2-10 — That ten-task window finished: nine proposals passed and entry 44597 failed. Inspection of its actual request and response showed a path-role mix-up: Luna put the child's reference path in source_path and a translation path in the parent's sense_path. Both selected phrases, 線路伝いに and 塀伝いに, have explicit German translations in the request. Do not repair this by swapping arbitrary strings.

WRI-CLASS2-11 — Response schema v5 restricts each candidate's source_path to its own translation/definition paths and sense_path to actual focal-entry sense paths, with null retained. Legacy schema v4 requests keep their original contract. Six classifier tests pass, including a regression rejecting reference-as-translation and translation-as-sense paths. Retried only entry 44597 with the new schema; inspect its attempt outcome before continuing the next window. Export integration remains unfinished.

WRI-CLASS2-12 — Retry 44597 passed structural validation: attempt `att-8049bc41d3cc4ec198e5d9387ecd3e50`, 14,505 input and 1,705 output tokens. This removes the path-contract failure, not the need for semantic review.

WRI-RENDER-1 — The shared structured_entry renderer now consumes localized accepted_examples. It places sense-bound examples inside the exact sense, respects pitch-group sense filters, and emits example-sentence / example-sentence-a / example-sentence-b selectors. Entry-wide examples remain entry-wide. Duplicate unit IDs, foreign parents and absent senses fail. German rendering uses source text; Russian rendering uses the target, restoring protected fragments. A fixture checks placement, filtering, language and parent validation. Five projection tests pass. Real scope-to-projection adoption, lexical grouping, final ZIP checks and actual Yomitan behavior remain unfinished.

WRI-CLASS2-13 — Full tests completed successfully after the renderer change, with three skips. Main-thread source review of the last ten entries found semantic issues despite schema success; see reports/wadoku_rich_classification_review.md. Prompt v6 addresses constructions incorrectly labelled pure inflections and clarifies example exclusions. A further window of ten pending entries is running with v6. Entries 34981, 39133 and uncertain aliases are not cleared for export.

WRI-CLASS2-14 — The v6 window finished with ten structurally valid proposals (entries 218053, 264963, 282227, 339198, 614795, 627246, 714571, 761205, 898729, 976142). No process from this window remains live. Review its semantic decisions before the next window. Prompt v6 is now the classifier default.

WRI-CLASS2-15 — Suffix-alias normalization now derives evidence_path from the exact non-display focal form/orth matching source_form, with an old/new audit record. Validation rejects invented or mismatched suffix evidence paths. This does not change ownership, infer an expansion, or clear lookup_needs_review. Six classifier tests pass. Previously stored proposals retain their original history and must be normalized/reviewed before adoption; they were not silently rewritten.

WRI-CLASS2-16 — Reviewed selected child-source meanings in the completed v6 window. WCR-7–WCR-9 record wrong or overly broad sense attachments and one unsuitable book-title example. These need explicit adoption corrections; the raw proposals remain intact. Started the next ten pending classification tasks. No new Russian translation or ZIP is claimed.

WRI-ADOPT-1 — Added adopt_examples to the shared projection module. It accepts an explicit reviewer/note and reasoned corrections keyed by exact candidate ID, validates the corrected contract, rejects unresolved decisions, and emits accepted/rejected candidates for prepare_projection. It preserves the original proposal and records its hash, request hash and corrections in the derived review. A fixture proves that a corrected sense reaches the example translation unit without changing the proposal; unknown candidate IDs and invalid senses fail. Six projection tests pass. This function is not yet wired to a scope-adoption command or applied to the real WCR corrections.

WRI-ADOPT-2 — Added the actual adopt-examples command to scripts/wadoku_rich.py. It binds the review to an exact proposal attempt, checks focal and candidate source identity, and stores a separate wadoku_example_adoption audit artifact. Applied reports/wadoku_example_review_614795.json to wadoku_rich_pilot: three selected examples, with appreciation moved to sense 4 and monetary valuation to sense 2. Artifact identity is 8935afa994e963131e9d04a93a67680544e30fb0dc43f6ecbe42a1e1eae423b9. Raw source and Luna proposal remain unchanged. Scope preparation still needs to load these artifacts and require current review identities.

WRI-ADOPT-3 — Added load_reviewed_examples with artifact-hash, current attempt/proposal, source-hash and exact candidate-coverage checks. On the real PostgreSQL pilot, repeating the 評価 review returned created=false; loading returned all nine candidate decisions and three accepted examples. Tests reject changed attempts and missing candidates. Scope-wide ownership adoption and preparation still need integration; this loader alone does not make the pilot ready.

WRI-ADOPT-4 — Applied reports/wadoku_example_review_627246.json to PostgreSQL. The title candidate is excluded; the two checked ordinary collocations remain selected. All ten candidate decisions remain in the derived artifact, identity bafb3ca7859a24a4b073c5ace252d8f58f74654ad888aa57b5f75fa6b7b08da0. The third display slot need not be filled merely to reach the maximum. Another ten-entry classification window is running; inspect terminal outcomes before further dispatch.

WRI-REPORT-1 — Added a read-only classification-report command to scripts/wadoku_rich.py. It does not migrate the database. For explicit scope entries it shows source meanings, ownership proposals, aliases, selected/unresolved examples with their actual source blocks, and candidate counts. It labels the output as a proposal, not approval. Exercised on three real pilot entries; WCR-11 records the findings. The report makes manual review smaller without changing translation requests or discarding source data.

WRI-ADOPT-5 — Read the complete focal/parent XML for 心変わりする (1836351) and 心変わり (9652262). Parent meanings already include betrayal/unfaithfulness; the child has explicit subentrytype=suru. Added adopt-ownership and applied the source-bound review in reports/wadoku_ownership_review_1836351.json, separate from the original Luna proposal. The stored decision is inherit_parent/shared_direct, with all child verb meanings required to remain. Audit identity: 2c161a46887d7f653738fb2932c97ff0f824af5da95e5aac2d6ba193530d2710. Actual shared-article export and lookup remain unimplemented/unverified. Another classification window is running.

WRI-GROUP-1 — Added ownership_groups: exact decision coverage, retained source members and lookup policies, missing-parent rejection and ownership-cycle detection. Tests distinguish independent phrases, pure inflections and shared direct aliases without discarding source members. This is graph preparation, not completed merging or pitch-aware export.

WRI-GROUP-2 — PostgreSQL currently has 75 proposals for 190 entries. The last classification window is terminal. Source-scope inspection found missing required ownership parents: 9652262 (reviewed suru merge), plus 8166358 and 5922712 for two still-disputed inheritance proposals. Their XML is in parent context but not in wadoku_scope_entry. Before scope-wide preparation, resolve ownership and create a new immutable scope with necessary owner dependencies within the authorized 150–200 entries. Do not mutate the old scope or discard accepted work; carry compatible results with explicit provenance. The current scope is not yet closed for ownership.

WRI-GROUP-3 — Extended inventory with retained-scope and required-owner-ids options. It retains every old selected entry/category, checks source/core identity and old entry hashes, and rejects extensions above the requested ceiling. Started a full-source inventory for 193 entries: original 190 plus the three identified owners. Output target is work/wadoku-xml/pilot-v6/scope-193.json. This creates a new scope, not an edit to the old one. Compatible proposal/review transfer still needs implementation; do not blindly rerun all classification or copy scope-bound candidate IDs.

WRI-GROUP-4 — The new inventory completed in 48.34 seconds: scope 71c1b53cd6556d7848de3d0b9b83b179b31e1392cb4501c9e733d221fa8c43db, 193 entries and 1,118 reverse candidates. Original core/categories and all 190 entries remain; three required owners were added. This is the new preparation target. Historical scope remains unchanged.

WRI-REUSE-1 — Implemented reuse-classification with exact focal/parent/candidate context and prompt comparison, explicit scope-bound ID remapping, current validation and audit provenance. Applied it to the new scope: 62 proposals transferred without model calls; 13 older-contract proposals were conservatively skipped for context mismatch. New-scope records retain the original attempt ID plus their new embedded request and source-record hash. Adoption/report readers now use that embedded request. Sixteen focused tests pass. Semantic review artifacts have not yet been transferred; do not assume that proposal reuse implies review adoption.

WRI-REUSE-2 — Investigated the 13 mismatches. Legacy JSON trees preserve whitespace after the closing entry element; reparsed XML does not. The comparison now ignores only whitespace-only root tail, not internal text/tails. It also compares the same compact candidate block fields while retaining source hashes, paths and protected fragments. A regression rejects changed internal text. All 13 remaining proposals transferred, bringing the new scope to 75 proposals without extra model calls. Started the next ten pending tasks on scope 71c1b53cd6556d7848de3d0b9b83b179b31e1392cb4501c9e733d221fa8c43db; use this scope for continued work.

WRI-REUSE-3 — Adoption commands now accept --to-scope for an existing source-bound review. They verify the exact original record hash against the target reuse provenance, recheck classification context, and remap example correction IDs through the corresponding candidate list. Applied all three existing reviews to the new scope: 評価 artifact 049b5e06f595cc9c7122b05700b1f8204c250837cbe88c0bc5af38ff9d0cf506; 素晴らしい f1d9ba3c04143d685cb406f85f1645adb7737758ea24fa98d78d8bb5174d4875; 心変わりする ownership f2a9a15845d3ba6a10c21844c94d7d888c0b1038c9cbce38dea3bffb28be5b47. No Luna calls were needed for review transfer. Old records remain unchanged.

WRI-ADOPT-6 — Reviewed a sample from the completed new-scope window (WCR-12). Adopted three 拾う examples in PostgreSQL, identity de08607c4c09b2e662b014d0d0ac95fb471a1e59ba546b49f2d2224a1524c2d9. Other reviewed entries need terminology/register context. Started the next ten classification tasks; no full-dictionary work or translation run was started.

WRI-CONTEXT-1 — Rich preparation now requires the unchanged full child source for every accepted example. It verifies the source identity and carries the full tree, including usage labels, definitions and explanations, into the example unit's local context. A fixture proves context preservation and rejection when source context is absent. Added a preparation-version field to run identity so older prepared runs cannot silently satisfy the new contract. Nine projection tests pass. Scope preparation must collect these child sources from the verified XML before calling prepare_run; no arbitrary glossary-only fallback is allowed.

WRI-CHECK-1 — Reran tests/test_wadoku_pipeline_postgresql.py against the isolated wadoku_rich_test database with its real connection configured: passed (not skipped). This covers preparation, manifests, fixture-response ingestion, rendering and window boundaries, not real translation quality. Reviewed the previous classification window's sample (WCR-13), adopted three 外れる examples in the new scope (8e5b26cfc468ab24f1afd9b40f2ef6cfeb7f1f4939333fbd3d56ce26f871b480), and started another ten pending classification tasks.

WRI-CONTEXT-2 — Added collect_example_sources: one XML pass for all selected children, with archive-XML hash checks before/after and exact per-child source-identity checks. Exercised it against the real XML and four PostgreSQL-reviewed parents: collected 11 child sources and prepared 11 example units carrying their full context. This was a read-only preparation check, not a saved translation run. Ten projection tests pass, including missing/changed child rejection. The current classification window completed with ten valid proposals; no process from that window remains live.

WRI-PROMPT-1 — Read Kolobok's translate_luna_v4.txt and Wadoku XML v4, then created translate_luna_wadoku_xml_ru_v5.txt for the shared rich contract. It retains sense-level synonym consolidation, natural Russian wording and typed output; adds explicit child-example context/path rules, register and title handling; and keeps metadata/ownership in the assembler. Existing prompts remain unchanged. The new translation prompt has not yet been used for a real translation run. After reviewing a sample of the previous classification window, started the next ten pending classifications on the 193-entry scope.

WRI-TRANSLATE-1 — Added wadoku_translate_window.py to exercise common PostgreSQL prepare_run, make_batches, claim, shared Luna dispatch and ingest_response on an explicit bounded diagnostic subset. Started it for 評価, 素晴らしい, 拾う and 外れる with their reviewed examples and full child sources. This is an integration/debug window, not a replacement for the full 193-entry pilot. The driver still needs production-grade failure recovery, complete request/schema accounting and shared window-ledger integration before broader use. Inspect the live session and saved attempts rather than restarting after an observation timeout.

WRI-TRANSLATE-2 — Diagnostic run 9 prepared four articles/33 units. First Luna attempt att-edf9c09a5a14467b9018d9afb1a245fd ingested four 素晴らしい units (24,672 input/696 output tokens). Its second sense includes ужасный/жуткий and needs semantic review; ingestion is not linguistic approval. The driver then stopped before the second CLI call because the byte-based reservation exceeded 96,000. Exact request+schema+margin+output bounds for the four batches are 86,729; 101,330; 77,294; 96,374. These fit the previously verified 128,000 reservation, not 96,000.

WRI-TRANSLATE-3 — Moved complete-request/schema budget checking before claim; added a checked context-budget option and a per-dispatch budget audit. Verified the old driver was terminal and the second response absent, recorded its claimed attempt as failed-before-dispatch, and returned only batch b-14dedf17e45548b5801f3757 to ready. Restarted the same idempotent diagnostic run with 128,000; the accepted first batch must not be resent. Remaining recovery and window-ledger integration still need work.

WRI-TRANSLATE-4 — Resume returned existing run 9 (created=false) and did not remake or resend its accepted batch. Attempt att-763c5a3ece2a4962b5c51856dd926601 ingested nine units with 29,066 input/1,084 output tokens. The driver remains live for remaining work. Investigated 素晴らしい against Japanese dictionary evidence; WTR9 records why the unqualified negative equivalents require clarification rather than automatic deletion.

WRI-TRANSLATE-5 — Session 68849 is now confirmed terminal with exit 0. Remaining attempts att-e14ce9c493364eec8c02af9c0ecaf6b9 and att-2dc9f853a8364adeae03a636b835e568 ingested 7 and 13 units. All 33 run-9 source/target pairs were read in the main thread. WTR9-4–8 record the findings and source checks. Do not restart this completed driver. Current scope coverage is 115 classification proposals out of 193 entries; proposals are not final semantic adoption. No final pilot archive exists yet.

WRI-REPAIR-1 — Extended replace_target for typed rich-v3 glossary arrays, shared validation/storage, row locking and existing append-only replacement history. The PostgreSQL fixture initially exposed missing deterministic acceptance in its setup; fixed the fixture by calling the existing common acceptance step, not by weakening replacement checks. Test now passes. Applied four recorded WTR9 repairs in run 9 after deterministic acceptance of its 33 units. Reviewed the prior classification sample (WCR-14) and started session 78223 for the next ten pending entries. No final ZIP yet; remaining work is classification/adoption, complete assembly/export and actual Yomitan QA, not further architecture expansion.

WRI-FOCUS-1 — User clarified that the primary deliverable is a debugged script-driven pipeline and improved prompts, not hand-polished entries. Run 9's manual targets cannot demonstrate model quality. Created immutable translation prompt v6 with general rules for Japanese valency, positive evaluation, natural impersonal examples, source quantities, duplicate context and honest uncertainty. The driver accepts --prompt and includes its hash in run identity. Started an unedited repeat of run 9 as run 11, session 29262. Compare new model answers against the original v5 answers (recover manual predecessors from history), not against hand-fixed text as if Luna produced it.

WRI-WINDOW-1 — Connected the translation driver to begin_window/window_batches/finish_window. It now freezes dispatch membership in PostgreSQL and saves an awaiting-analysis report before another window can start. Existing processes started before this code change do not exercise that integration. Eleven focused projection/PostgreSQL tests pass; live integration still needs verification. Classification session 94366 completed, bringing proposal coverage to 135/193. Translation session 24700 runs the earlier v5 ten-entry window as run 10; session 29262 runs the v6 regression as run 11. Do not relaunch either until terminal state is verified.

WRI-WINDOW-2 — Both sessions 24700 and 29262 are now confirmed terminal with exit 0. Run 10 contains 21 units for ten entries; run 11 contains all 33 regression units. Read all run-11 outputs unchanged and recorded prompt-level results in WR6. Added window-report/analyze-window commands and source/target context to the review queue. Continuation rejects unreviewed uncertain translations; the queue uses the latest translation per unit so superseded attempts do not remain forever. Reran eleven focused tests against real PostgreSQL successfully. No live worker remains from these sessions. Next work should exercise the integrated script boundary and contextual review/repair across the representative pilot, not manually polish individual entries.

WRI-WINDOW-3 — Live script-boundary test uses run 12 (ten pilot entries, 21 units, v6). Session 66666 completed exactly one batch and persisted window 1 as awaiting_analysis. Calling begin_window before analysis rejected continuation. The CLI accepted the source-bound analysis in reports/wadoku_run12_window1_analysis.json. Session 93530 resumed the same run (created=false), created no new manifests, translated only the next batch and stopped at window 2. Both sessions are terminal. Window 2 analysis is recorded in reports/wadoku_run12_window2_analysis.json. No target was manually edited.

WRI-WINDOW-4 — Added --run-id to resume frozen PostgreSQL inputs without re-reading the full XML. It checks the run pipeline and exact prompt hash and rejects simultaneous scope/entry arguments. Real PostgreSQL tests cover successful resume, absent run and wrong prompt. The report now includes successful source/target pairs as well as uncertainty, so the orchestrator can inspect a sample through the same command. Started session 41464 with --run-id 12 --max-batches 8: it immediately reused run 12 and dispatched the remaining bounded set without rebuilding input. Confirm this handle before any further run-12 dispatch. Full 193-entry processing and contextual repair remain unfinished; the ten-entry window test does not replace them.

WRI-REVIEW-1 — Adapted the existing shared review module to rich Wadoku instead of adding a second replacement database path. Review manifests now retain wadoku-xml-v3/schema 2, full projection context and a bound optional review-prompt hash. Batch preparation is idempotent; ingestion checks the frozen candidate, prompt provenance, typed replacements and protected tokens. Added review_luna_wadoku_xml_ru_v1.txt and the bounded wadoku_review_window.py CLI using shared claim/dispatch/ingest. Seven existing pipeline and real PostgreSQL tests pass, including rich array replacement by a separate reviewer. Started session 83315 for all 33 run-11 units, four review batches. No manual target patches are part of this pass. Failure recovery and broader pilot coverage still need completion.

WRI-WINDOW-5 — Session 41464 completed with exit 0. Run 12 now contains all 21 units across ten entries and three persisted windows; window 3 awaits analysis. This verifies bounded continuation without repeat dispatch for the sample. Its uncertainty queue is real and must be analyzed, not silently treated as release approval.

WRI-REVIEW-2 — Session 83315 completed with exit 0: all 33 run-11 units reviewed, two replacements and no adjudication. Main-thread inspection found that review v1 accepted the ambiguous evaluation in 素晴らしい by matching German synonyms and retained awkward wording in examples/technical text. This is a failed quality probe, not pilot approval. Added review prompt v2 requiring explicit treatment of candidate warnings and Japanese evidence, plus natural collocation/domain checks. Added explicit --recheck with version-bound manifests, newest-candidate selection, preservation of review history and revocation of acceptance when a new review needs adjudication. Seven tests pass, including real PostgreSQL recheck and acceptance revocation. Session 32329 is the live four-batch recheck of run 11; inspect it before another recheck.

WRI-CLASSIFY-7 — Added classification prompt v7 for the general error found in WCR-14: compare each example against every focal sense, never copy child sense numbering or default to sense 1, and justify the selected parent meaning. Existing v6 decisions are not silently relabelled as v7. Started a bounded ten-entry v7 window for the remaining 58 pending scope entries. This tests broader pilot coverage while contextual review runs; it is not manual correction of selected article text.

WRI-CHECK-2 — Full pytest run completed with exit 0 and three skips; the separately configured PostgreSQL test passed alongside six existing pipeline tests. Classification v7 session is 57039 and review-v2 recheck session is 32329; both were confirmed live at the last poll. Recheck v2 now routes the ambiguous 素晴らしい sense to needs_adjudication and revokes acceptance as intended. However, it also regressed the positive nuance of würdigen, replacing давать высокую оценку достижениям with neutral оценивать достижения. Therefore review v2 is not a blanket quality approval: review decisions themselves need window-level error analysis, especially overcorrection of nuance. Do not manually patch that target and present it as model improvement.

WRI-REVIEW-3 — Session 32329 is now terminal, exit 0: review v2 accepted/replaced 32 units and left one needs_adjudication. Created review prompt v3 to preserve evaluative/modal meaning inherent in source words rather than requiring a separate matching modifier. Test it on a fresh ten-entry set: session 27774 reviews run 12, 21 units, explicit v3 prompt. Initial batches are ingested; do not claim all ten reviewed yet or restart this live handle. Its quality must be assessed from replacements and a sample, not acceptance counts.

WRI-CLASSIFY-8 — Session 57039 completed, reaching 145/193 proposals. Main-thread sample of 場所, 引き and 捨てる shows specific parent-sense mapping (seat sense 3, support sense 5, and distinct senses for abandoning family, discarding rubbish and giving up hope). English reasons remain a presentation inconsistency in some proposals but do not enter dictionary text. Started the next ten v7 classifications, session 81728; nine results were delivered at the last poll. No new manual translation replacements were made.

WRI-SCOPE-PREP-1 — Separated translation-candidate preparation from final example adoption. --candidate-scope includes the entire frozen 150–200-entry pilot, requires all classification records, verifies source/candidate identities, keeps unresolved decisions visible and sets release_approved=false. It does not create manual-review/adoption events. --prepare-only prepares without dispatch. Existing strict reviewed-example preparation and frozen-run resume remain available. Ten projection tests pass, including preservation of unresolved candidate state and absence of fabricated review records. Real prepare-only correctly refused the current incomplete scope with 38 missing classifications, before preparing a run. Final release still requires resolved grouping, examples and lookup behavior; this intermediate path is not a release bypass.

WRI-CLASSIFY-9 — Session 81728 completed: 155/193 proposals. Reviewed samples 氷, 隙間 and 国: selected examples match their parent meanings; 国 uses senses 3, 1 and 2 rather than a first-sense default. Started the next ten pending v7 tasks. Session 27774 also completed: all 21 run-12 units passed review v3, with one replacement. This proves the scripted review completed on a fresh set, not universal quality; the replacement and warning resolutions still need window analysis.

WRI-SCOPE-PREP-2 — Read-only test of the new loader covered every then-classified entry: 157 entries, 232 selected examples, 11 unresolved and 671 rejected, zero source/contract errors. These are candidate states, not final editorial approvals. Classification session 48933 remains the active next-ten window; its early results explain the increase from 155 to 157. No translation run for the whole scope has been created yet.

WRI-CONTEXT-3 — New preparation version v3 marks selected-example envelopes. Translation/review requests no longer carry full rejected-example inventories or duplicate child trees; selected example units retain their complete child tree in local_context, and full inventories stay in frozen PostgreSQL projections. Compact state lists keep unresolved candidates visible. Older projections without this envelope version retain their old request shape. Ten tests pass, including child-context preservation, immutable projection data and legacy behavior. This is context reduction, not deletion of source/example coverage.

WRI-CLASSIFY-10 — Session 48933 completed, reaching 165/193 proposals. Sample inspection found two remaining general selection problems: mapping a focal noun by the whole phrase's action (糸巻きに巻く belongs to the spool/object sense, not winding-as-action), and selecting a bare causative form as an example (逝かせる). The sports example ヒット3本に抑える also has conflicting Japanese/German evidence (hits versus points) that must reach semantic review. Record these as classification/source-review issues; do not silently approve or hand-patch them. Started the next ten pending v7 tasks; candidate preparation remains non-release.

WRI-CLASSIFY-11 — Session 38694 completed, reaching 175/193 proposals. Reviewed 頂く, 言い方 and 壁. 壁 correctly separates literal wall and figurative barrier examples. 頂く still misassigns 雪を頂く山 to receiving instead of the wearing/carrying sense; add metaphorical focal-word interpretation to the same sense-attachment issue class. No manual content repair was applied. Started the next ten pending classifications; the remaining scope is eighteen entries.

WRI-FULL-1 — PostgreSQL showed 185 proposals after the old session handle disappeared. Session 31419 processed the final eight with classification v7 and exited successfully: all 193 entries now have proposals. This is classification coverage, not semantic approval.

WRI-FULL-2 — Full preparation exposed an integration error: the strict projection version contract rejected an extra classification_provenance key. The driver now puts the hash of all frozen classification records in the existing morphology version field. Candidate run limits also record the actual requested context budget. Repeated preparation succeeded as run 15: 193 articles, 940 units, 193 batches, identity be23ec6ebad258f2afea0dacfc63c9227f739900e6cdd331ae50f9fcf9209871. No entry was dropped or manually translated.

WRI-FULL-3 — Checked every run-15 request with its output schema, prompt, 32,768 runtime margin and 12,000 output reserve. Maximum reservation is 115,172; all 193 fit the configured 128,000 ceiling. Twenty-three classification/projection/shared-pipeline tests pass. Session 35338 started the first ten-batch translation window with v6; at the last poll it remained live on attempt att-93160801f76240d18a4d5cdb0fb195eb. Inspect this handle before resuming. Full translation, review, assembly and Yomitan validation remain unfinished.
